#include "holberton.h"
/**
 * print_rev - Prints a string followed by a new line
 * @s: string to print
 * Return: a
 */
void print_rev(char *s)
{
int len;
	int _strlen(char *s)
	{
		while (s[len] != '\0')
		{
		len++;
		}
	len = len;
	int a
	for (a = len; a = 0; a--)
	{
		_putchar(s[a]);
	}
	_putchar('\n');
	}
}

#include "holberton.h"
/**
 * print_rev - Prints a string followed by a new line
 * @s: string to print
 * Return: a
 */
void print_rev(char *s)
{
	int a;

	for (a = '\0'; a = 0; --a)
	{
		_putchar(str[a]);
	}
	_putchar('\n');
}

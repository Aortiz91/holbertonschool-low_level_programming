#include "holberton.h"
/**
 * _abs - Function that descrribes the absolute value of a integer
 * @n: integer
 * Description: Absolute value
 * Return: n if is positive or -n if is negative
 */
int _abs(int n);
{
	if (n < 0)
	return (-n);
	else
	return (n);
}
